[pytest]
# Put the repo root on sys.path so `import app` works from tests/.
pythonpath = .
# Only collect the tests/ package. This keeps the manual, network-hitting
# helper script test_api_connection.py at the repo root out of the suite.
testpaths = tests
